# Light DevOps 前端开发代理与生产构建配置模板。

# REST API 基础路径，默认 /api/v1。
VITE_API_BASE_URL=/api/v1

# Vite 开发服务器代理目标地址（指向本地 API）。
VITE_DEV_API_TARGET=http://127.0.0.1:8000

# 生产构建是否生成 sourcemap，默认关闭。
VITE_BUILD_SOURCEMAP=false
